---
import { marked } from "marked";
import Layout from "../../../layouts/Layout.astro";
import readme from "../../../../../../packages/a11y/README.md?raw";

const readmeHtml = await marked(readme);
---

<Layout title="a11y" description="@bambi-ui/a11y — accessibility primitives for React: focus trap, focus ring, keyboard interaction, live region announcements, and WCAG-compliant utilities." keywords="bambi-ui a11y, react accessibility, focus trap, focus ring, keyboard navigation, aria live region, wcag primitives, typescript">
  <h1 class="mb-2 text-3xl font-bold">@bambi-ui/a11y</h1>
  <p class="mb-10 text-muted-foreground">
    Accessibility primitives used across all <code class="rounded bg-muted px-1 text-xs">@bambi-ui</code> components.
  </p>

  <div class="prose">
    <Fragment set:html={readmeHtml} />
  </div>
</Layout>
